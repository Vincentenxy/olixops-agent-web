# 待办与接入记录

更新日期：2026-09-08。本文同时记录基础能力和未决事项；每次完成条目时补充实际验证结果。前端状态、后端接口可用性与真实部署能力分别记录。

## 本轮基础能力

- [x] React + TypeScript + Vite SPA 初始化；Node.js 24、pnpm 10.11.1 与依赖锁定。
- [x] React Router 页面路由、TanStack Query 数据层和 Ant Design 基础主题。
- [x] 中文紧凑工作台、手动保存/恢复/清空本地草稿、任务未接入空态和连接状态入口。
- [x] API 统一响应与错误适配、32 位请求 ID、公开健康检查请求；健康检查通过仍保持部署禁用。
- [x] 多阶段 Dockerfile、Nginx 静态托管与 SPA 回退；`/healthz` 检查静态站点，`/api/` 暂时返回 HTTP 503。
- [x] 将本地 UI 规范完整整理为项目内 [UI 开发规范](docs/ui-development-standards.md)，57 条规则全部保留；用通用三栏示例替换旧项目名称。
- [x] 建立 [AGENTS.md](AGENTS.md) 短指针及 [前端接口契约](docs/frontend-development.md)，前端开发无需个人技能路径。
- [x] 完成 lint、格式检查、类型检查、14 项测试和生产构建。
- [x] 完成 1280 × 720 桌面与 390 × 844 窄屏检查，无横向溢出；验证草稿保存、重载恢复、恢复已保存和清空。
- [x] 通过最小 FastAPI 服务验证真实健康检查：后端未启动时出现连接失败，启动后检查成功；任务仍显示未接入，部署保持禁用。
- [x] 配置 CI 执行 `pnpm check` 与 Docker 构建，Actions 使用完整 commit SHA 固定；首次远程 CI 已通过，详见验证记录。
- [x] GitHub CI 中 Docker 镜像构建通过；本地构建仍受 Docker Hub 基础镜像请求超时影响。
- [ ] 完成容器运行验收，验证静态页面、路由回退、`/healthz` 与 `/api/` 行为。

## 功能接入

| 条目                      | 影响                                           | 暂定行为                                                 | 完成条件                                                                      |
| ------------------------- | ---------------------------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------- |
| 真实任务 API 与 OpenAPI   | 需求无法转为部署任务，任务列表和详情无真实数据 | 保留草稿，显示空态/未连接态；不生成模拟部署成功记录      | 后端提供创建、查询、详情和动作协议，前端按契约完成联调                        |
| MCP 查询                  | 无法读取可用环境、资源和现有应用信息           | 明确标注查询能力未接入，配置输入不视为已验证             | 确定查询 schema、访问范围与失败形式，验证真实返回数据                         |
| 部署目标与模板            | 无法可靠推导部署位置、资源与参数               | 需求文本仅作为草稿；未取得真实选项前保持相应配置不可执行 | 确定目标类型、模板版本、参数约束与默认规则，并由后端校验                      |
| 应用代码与构建产物        | 无法明确需要部署的代码/镜像版本                | 前端不直接构建镜像或运行 Terraform                       | 确定代码/镜像输入协议、构建状态及固定版本标识                                 |
| 任务状态与进度流          | 无法展示持续执行、重连或取消后的实际进度       | 不从连接状态推断部署结果；后端执行独立于页面生命周期     | 约定任务状态机、步骤、错误结构、轮询/SSE、事件 ID、日志游标及断线恢复         |
| Terraform plan 与执行动作 | 计划展示、审核和 apply 缺少可执行契约          | 前端不自行授权 apply；按环境授权策略决定是否出现审核流程 | 展示真实 plan；动作绑定计划版本、任务和服务端权限；验证计划变更及重复请求处理 |
| 结果与产物                | 无法展示真实访问地址、执行日志和资源结果       | 只展示服务端确认的数据，不推断成功                       | 约定结果 schema、日志分页/流、下载权限、敏感字段过滤和保留期                  |

## 身份与部署未决项

| 条目                           | 影响                                   | 暂定行为                                                                                   | 完成条件                                                                   |
| ------------------------------ | -------------------------------------- | ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------- |
| JWT 登录来源与会话             | 受保护 API 缺少登录、刷新和退出流程    | 健康检查使用公开接口；预留内存 token provider，尚未获取真实 JWT                            | 明确 SSO/登录入口、token 获取与存储、刷新/退出，以及 401/403 UI 行为       |
| CORS 与 Cookie                 | 跨域认证请求及请求 ID 读取可能失败     | 浏览器固定同源 `/api`，请求使用 `credentials: 'omit'`；当前不发送 Cookie                   | 确定 Origin、允许头、credentials、暴露 `X-Request-Id` 及生产同源策略       |
| 多用户/多环境权限              | 页面可见动作可能与资源权限不一致       | 操作权限以服务端为准，不以客户端 JWT 解析结果授权                                          | 确定权限查询/动作字段，验证无权限、过期和环境切换                          |
| Docker 容器运行验证            | 尚未验证构建产物能在容器中实际运行     | GitHub CI 镜像构建通过；本地 Docker Hub 基础镜像请求超时，尚未完成容器启动验收             | 镜像源可访问后重新构建并验证静态页面、路由回退、`/healthz` 与 `/api/` 行为 |
| Docker 生产 API 代理与集群目标 | 容器尚不能请求真实 API，流式行为未确认 | 已有静态托管和 SPA 回退；生产 `/api/` 固定返回 503，开发通过 shell `API_PROXY_TARGET` 转发 | 明确真实 API 反代、Kubernetes 入口、超时及 SSE 缓冲配置并联调              |
| 配置持久化与草稿范围           | 可能混淆本地草稿与团队共享配置         | 草稿手动保存到当前浏览器 `localStorage`；设置页仅查看状态，API 目标由部署配置管理          | 确定跨设备、用户切换、清理和迁移规则；敏感配置由后端管理                   |

## 验证记录

- 文档：UI 原始规范 57 条列表规则已逐条比对，无遗漏；个人绝对路径和旧项目三栏名称已清理。
- 代码：`pnpm check` 通过，包含 ESLint、Prettier、TypeScript、Vitest 14 项测试（9 项 API、5 项草稿交互）和 Vite 生产构建。
- 界面：1280 × 720 桌面与 390 × 844 窄屏无横向溢出；草稿保存、刷新后恢复、恢复已保存及清空已验证。
- 真实 API：后端未启动时，开发代理返回 502，界面呈现连接失败；启动后端后，健康检查成功。任务页仍显示未接入，“提交部署”仍禁用。
- 远程 CI：[Frontend CI #1](https://github.com/Vincentenxy/olixops-agent-web/actions/runs/34227479432) 对提交 `e9d7f5b` 执行成功，依赖安装、`pnpm check` 和 Docker 镜像构建全部通过。
- Docker：本地基础镜像 `node:24-alpine` / `nginx:stable-alpine` 的 Docker Hub HEAD 请求超时，构建未进入应用阶段；GitHub CI 已成功构建镜像，容器运行验收仍未完成。镜像源恢复后在本地重试。
- 后端：此次联调只验证最小 FastAPI 健康接口；任务、MCP、Terraform plan/apply 与真实部署尚未接入。
