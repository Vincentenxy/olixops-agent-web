# 待办与接入记录

更新日期：2026-09-10。已提供平台基础能力与 LangGraph 方案分析界面，真实资源部署仍待接入。业务开发暂不使用 Pulumi，既有代码、依赖及基础诊断保留；历史验证记录不代表当前部署路线。

## 已确认开发计划与前端进度

完整顺序和验收条件以 [Redis 开发计划](../olixops-agent/design/redis-development-plan.md)（[独立检出入口](https://github.com/Vincentenxy/olixops-agent/blob/main/design/redis-development-plan.md)）为准，实施前同时核对[后端 todo](../olixops-agent/todo.md)（[远程入口](https://github.com/Vincentenxy/olixops-agent/blob/main/todo.md)）。本节仅记录前端工作，计划写入文档不表示功能验收完成。

- [x] 记录用户确认的开发路线，并在 AGENTS.md 建立后续 AI 的必读入口。
- [ ] 阶段 5：接入固定参数创建、配置差异和发布入口；以阶段 3/4 的实例、配置版本、任务 API 契约为依据，显示确定 Git commit 对应的执行结果。
- [ ] 阶段 5：实例详情、历史配置版本、轮询进度、失败原因和重试；明确区分方案就绪、配置已保存、正在部署、验证失败和服务可用。
- [ ] 阶段 5：固定参数创建完成后接入资源参数修改和删除，呈现后端规定的 PVC 保留策略。
- [ ] 阶段 5：使用真实后端和 POC 集群验收登录、发布、失败处理、重试与经过验证的 TCPRoute 访问结果，并完成桌面/窄屏验收。
- [ ] 阶段 6：现有 LangGraph 对话复用固定参数发布接口，支持需求补齐、环境校验反馈和部署结果，使用同一配置与任务契约。

当前依赖：真实集群查询、标准 Redis 模板、配置 Git 仓库及持久部署 Worker 尚未接通。可按已确定的后端契约先做独立设计与组件工作；缺少环境或接口时记录具体阻塞、影响和完成条件，继续无依赖工作，测试替身通过只能记为测试结果。

本次计划落档验证：4 个修改文档的 Prettier 与 `git diff --check` 通过；后端统一检查两端 80 个本地链接及章节引用均有效。仅修改文档，未运行应用测试、修改业务代码或连接集群。

## LangGraph 方案分析

- [x] 工作台用一句话创建 Redis / PostgreSQL 分析方案；只发送需求正文，无需仓库。
- [x] 分析服务状态检查、最近 50 个任务列表、持久对话详情、queued 轮询与终态停止。
- [x] 补充和修改需求、版本冲突刷新与保留输入、失败手动重试、创建/续聊的 UUID 幂等键。
- [x] 结构化 Kubernetes 部署规格；方案就绪明确标注尚未部署，真实部署按钮仍禁用。
- [x] 查询按会话与用户隔离，固定业务错误提示，避免展示模型供应商原始异常。
- [x] 本轮 `pnpm check` 通过，60 项测试覆盖基础能力和 Agent 分析交互。
- [x] 浏览器真实分析流程、刷新恢复、方案列表及 390×844 窄屏验收通过；模型及后端验证边界见下方记录。

## 已实现的基础能力

- [x] React + TypeScript + Vite、React Router、TanStack Query、Ant Design 与依赖锁定。
- [x] 项目内完整 UI 开发规范与 AGENTS.md 开发入口。
- [x] 真实用户名/密码登录、登录错误、受保护路由、站内返回路径、头部身份与退出。
- [x] 内存 Access token + HttpOnly Cookie 恢复；StrictMode 单次恢复、并发刷新合并、401 最多重试一次。
- [x] 退出与迟到登录/刷新响应隔离；清空查询缓存和用户草稿，后端撤销失败提供重试退出入口。
- [x] 账户设置、12–128 字符密码与确认校验、改密成功重新登录；旧密码错误不退出当前身份。
- [x] 用户 ID 分区的非敏感草稿；旧版无所属用户草稿不迁移。
- [x] 真实数据库、Redis、认证和 Pulumi SDK/CLI 状态页；部署仍保持禁用。
- [x] 同源 API、JSON 错误封装、请求 ID、超时与取消、错误响应运行时验证。
- [x] Nginx 同源 API 代理模板，API_UPSTREAM 可配置，保留 SPA 回退、静态缓存和 /healthz。
- [x] 认证/竞态/接口/草稿自动化测试、lint 与类型检查通过。
- [x] 完成本轮全部格式检查与生产构建。
- [x] 完成本轮真实后端浏览器联调和桌面/窄屏验收。
- [x] 实际 Nginx 模板配合缓存镜像完成本地路由、同源 Cookie 与登录验收；生产镜像启动结果以本轮 CI 为准。
- [x] [Frontend CI](https://github.com/Vincentenxy/olixops-agent-web/actions/runs/34308320930) 在提交 `26e6fc4` 上全部通过：质量检查、38 项测试、生产镜像构建、容器健康检查、SPA 路由、静态资源 404 和 API 上游路由。

## 业务待接入

| 条目           | 当前行为                                                     | 完成条件                                              |
| -------------- | ------------------------------------------------------------ | ----------------------------------------------------- |
| MCP 环境查询   | Agent 已整理需求，尚未读取真实环境选项                       | 明确 MCP 查询 schema、权限和失败契约                  |
| 部署任务 API   | 已接入分析会话创建、列表、详情、补充和重试；没有资源执行     | 明确真实部署创建、取消、状态机与幂等契约              |
| Git 配置与发布 | 尚无实例配置差异、Git commit 和发布入口；Pulumi 基础诊断保留 | 按计划阶段 3–5 接通不可变配置版本、差异与指定版本发布 |
| 构建与目标     | 不推断代码/镜像版本及基础设施目标                            | 明确构建产物、部署平台、资源参数与默认规则            |
| 实时进度与产物 | 尚无部署日志、实际访问地址                                   | 明确轮询/SSE、断线恢复、事件游标、结果和下载权限      |

## 后续平台事项

| 条目                  | 当前行为                                                    | 完成条件                                                  |
| --------------------- | ----------------------------------------------------------- | --------------------------------------------------------- |
| SSO / 用户管理 / RBAC | 本地账号由后端 CLI 创建；前端显示管理员标记，权限由后端判断 | 明确外部身份源、用户管理和资源动作权限                    |
| 多标签页会话协调      | 每个标签页独立内存会话；失效由下一次请求发现                | 设计跨标签页刷新互斥、退出广播并验证 Cookie 旋转竞态      |
| 草稿服务端同步        | 当前用户当前浏览器手动保存，退出清除                        | 明确跨设备需求、权限、迁移与保留策略                      |
| 生产入口              | Nginx API_UPSTREAM 同源转发                                 | 按环境完成 TLS、Secure Cookie、Origin、可信代理与超时配置 |
| 账号找回              | 页面提示联系管理员，无自助找回                              | 明确找回凭证、通知渠道和审计流程                          |

## 2026-09-10 LangGraph 验证

- 自动化：固定 pnpm 10.11.1、Node.js 24.19.0，`pnpm install --frozen-lockfile` 与 `pnpm check` 通过；共 60 项测试，新增 12 项 Agent 响应/输入/错误边界测试和 10 项分析交互测试。包含需求创建、未配置禁用、幂等键复用、版本冲突、编辑版本绑定、queued 轮询停止、失败重试、会话恢复及身份变化后的迟到响应隔离。
- HTTP 联调：通过实际 Vite 代理连接临时 FastAPI，验证登录、创建、追问命名空间、补充后生成 Redis 7.4 / 10 GiB 与 PostgreSQL 16 / 20 GiB 规格、列表查询、Cookie 刷新后读取持久对话和退出。
- 浏览器：811 px 实际窗口验证登录、无仓库需求提交、追问、补充后 ready 两服务、页面刷新恢复登录与对话、列表中的持久任务；1280×720 详情双栏和 390×844 详情、服务规格及列表截图检查通过，两种视口的 `documentElement.scrollWidth` 与 `clientWidth` 分别相等（1280 / 390），控制台无 warn/error。测试账号已退出，临时浏览器已关闭。
- 验证边界：认证、业务 API、SQLite 业务库、LangGraph checkpoint 与后台处理使用真实实现；模型使用固定测试替身。未验证外部模型服务、真实 MCP 查询、Kubernetes 资源或 Pulumi 部署，不以方案 ready 作为部署成功。

## 2026-09-09 基础能力验证

- 本轮自动化测试：38 项通过，包括 7 项认证页面、17 项会话/竞态/重定向、9 项 API、5 项草稿。验证失败登录、刷新恢复、改密、并发 401、退出等待在途响应、跨账号刷新拒绝及旧请求不重放、阻止旧私有数据返回以及缓存清理。
- 本轮 `pnpm check` 通过：ESLint、Prettier、TypeScript、38 项 Vitest 与 Vite 生产构建。真实浏览器验证错误/成功登录、刷新恢复、改密后会话失效和新密码登录、退出后刷新保护，以及数据库/Redis/认证/Pulumi 四项正常状态。改密实际写操作通过 HTTP 联调执行，表单提交路径由组件测试覆盖。
- 历史基线：2026-09-08 的 14 项测试与 1280×720 / 390×844 界面验收通过；该结果不代表本轮认证页面已完成真实联调。
- 历史 CI：[Frontend CI #1](https://github.com/Vincentenxy/olixops-agent-web/actions/runs/34227479432) 对初始化提交成功构建 Docker 镜像。
- 历史记录：已接续远程 `c440680` 的 Pulumi 选型更新，当时记录了 preview/up、审核绑定及 update plan 与保存计划差异约定；当时仅文档调整，新增基础能力不代表业务部署已完成。2026-09-10 用户决定业务开发暂不使用 Pulumi，当前路线以 Redis 开发计划为准。

- 本轮界面验收：1280×720 桌面、390×844 登录与账户页无横向溢出，操作可达；浏览器控制台无 warn/error。
- 本地容器：PostgreSQL/Redis 实际运行正常；完整应用构建受 Docker Hub 获取 Python 镜像 token 超时影响。使用缓存 Nginx 1.27.5 镜像和本项目实际模板，验证健康检查 200、SPA 路由 200、缺失静态资源 404、不可用 API 上游 502；再代理真实后端完成登录、身份/状态查询、Cookie 刷新、退出及退出后 Bearer/Cookie 均拒绝。临时验证容器已删除。此结果验证模板与代理集成，生产镜像运行以 CI 为准。
- [首次基础能力 CI](https://github.com/Vincentenxy/olixops-agent-web/actions/runs/34307173544) 的质量检查和生产镜像构建通过，启动健康探测因连接重置返回 curl 56；已为启动探测补充所有错误重试、时间上限和失败日志，后续路径状态断言保留。

- 2026-09-09 安装排查：npm 11.3.0 对现有 `.pnpm` 目录执行离线 dry-run，复现 `Link.matches` 的 null 异常。改用项目固定的 pnpm 10.11.1 执行 `pnpm install --frozen-lockfile --offline` 成功，未删除依赖或修改锁文件；随后 `pnpm check` 的 lint、格式、类型、38 项测试和生产构建全部通过。README 已补充包管理器约定及恢复命令。
